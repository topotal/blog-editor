import React from 'react';
import {shallow} from 'enzyme';
import Login from '../views/login/Login.js';

describe('App', () => {
  it('should render login page in initial state', () => {
    expect(1).toBe(1);
  });
})
